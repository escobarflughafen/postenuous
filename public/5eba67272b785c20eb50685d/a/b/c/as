#include <stdio.h>
#include <string.h>


int main(int argc, char** argv) 
{
    if(argc < 3) {
        return 0;
    }
    printf("strcmp(%c%s%c, %c%s%c) -> %d\n", '"', argv[1], '"', '"', argv[2], '"', strcmp(argv[1], argv[2]));
    return 0;
}
